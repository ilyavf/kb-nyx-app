/**
 * Signup controller
 *
 * @author      TonyTong
 * @date        March 2014
 */

(function (define, angular) {
    'use strict';

    define([], function () {

        console.log('[Auth.LoginController] loaded');

        var SignupController = function ($scope, currentUser) {
            console.log('[Auth.SignupController] init');

        };

        return SignupController;
    });

}(define, angular));